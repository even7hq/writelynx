---
title: Regras custom
---

## Arquivo

`writelynx --rules ./tenant.json` ou `WRITELYNX_RULES`.

## API (JS)

`loadRulesFromFile`, `registerPatternRule`, `unregisterRule`, `reload`.

## API (Lua)

`writelynx.load(path)`, `writelynx.load_json(table)`, `writelynx.reload()`.
