---
title: Validador mod11
---

Usado para CPF e CNPJ. Referência em `validators.json` como `cpf_mod11` / `cnpj_mod11`.

Campos: `digitLength`, `rejectAllSame`, `checks[]` com `weights` e `checkIndex`.
