# @writelynx/builtin-rules

Built-in validation specs and redaction rules for WriteLynx.

- `validation/validators.json` - mod11, luhn
- `redaction/field.rules.json` - sensitive field names
- `redaction/pattern.rules.json` - content patterns

Use `getBuiltinRuleset()` from the compiled package.
